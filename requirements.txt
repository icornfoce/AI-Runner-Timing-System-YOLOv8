# RunnerTrack AI — Hybrid-Mode Dependencies
# Backend: GAS v6 (apps_script/Code.gs).
# Local server: web_app.py serves templates AND POST /analyze (YOLOv8 + PaddleOCR).
# Standalone alternative: checkpoint_camera.py (same AI, headless cv2 loop).
# Install: pip install -r requirements.txt

opencv-python
flask
ultralytics
paddleocr

# paddlepaddle: pick CPU or GPU build for your machine.
# CPU:  pip install paddlepaddle
# GPU:  pip install paddlepaddle-gpu
paddlepaddle

requests
numpy
